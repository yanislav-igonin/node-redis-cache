version: '3.7'

services:
  app:
    build:
      context: .
      dockerfile: development.Dockerfile
    environment:
      - NODE_ENV=development
      - REDIS_HOST=redis
    volumes:
      - ./src:/usr/src/app/src
    ports:
      - 9229:9229

  redis:
    image: redis:alpine